# Telegram Premium Sync - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    HollowScan Mobile App                    │
│                   (React Native / Expo)                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              ProfileScreen.js                        │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  State:                                        │  │  │
│  │  │  - telegramChatId: number                     │  │  │
│  │  │  - isPremium: boolean                         │  │  │
│  │  │  - premiumUntil: ISO8601 string              │  │  │
│  │  │  - telegramLinked: boolean                   │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  handleTelegramLink()                          │  │  │
│  │  │  1. Validate inputs                            │  │  │
│  │  │  2. Call API endpoint                          │  │  │
│  │  │  3. Update state with response                 │  │  │
│  │  │  4. Show premium alert if premium             │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  UI Components:                               │  │  │
│  │  │  - Telegram Modal (input chat_id + username)  │  │  │
│  │  │  - Integrations Section (shows status)        │  │  │
│  │  │  - Premium Badge (if applicable)              │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────┬───────────────────────┘
                                      │
                                      │ API Call
                                      │ POST /v1/user/telegram/link
                                      ▼
┌─────────────────────────────────────────────────────────────┐
│                       FastAPI Backend                       │
│                      (main_api.py)                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  POST /v1/user/telegram/link                         │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 1. Parse Inputs:                              │  │  │
│  │  │    - user_id (query param)                    │  │  │
│  │  │    - telegram_chat_id (query param)           │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 2. Load Telegram Bot Users:                   │  │  │
│  │  │    - Open data/bot_users.json                 │  │  │
│  │  │    - Look up: telegram_users[str(chat_id)]   │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 3. Check Premium Status:                       │  │  │
│  │  │    - Get user_data['expiry']                  │  │  │
│  │  │    - Parse ISO8601 timestamp                  │  │  │
│  │  │    - Compare: expiry > now()?                 │  │  │
│  │  │    - Result: is_telegram_premium (bool)       │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 4. Update App User Database:                  │  │  │
│  │  │                                                │  │  │
│  │  │  If Premium:                                  │  │  │
│  │  │  PATCH /users?id=eq.{user_id}                │  │  │
│  │  │  {                                             │  │  │
│  │  │    "subscription_status": "active",           │  │  │
│  │  │    "premium_from_telegram": true,             │  │  │
│  │  │    "telegram_chat_id": {chat_id},            │  │  │
│  │  │    "telegram_premium_until": {expiry},        │  │  │
│  │  │    "linked_at": {now}                         │  │  │
│  │  │  }                                             │  │  │
│  │  │                                                │  │  │
│  │  │  If Not Premium:                              │  │  │
│  │  │  PATCH /users?id=eq.{user_id}                │  │  │
│  │  │  {                                             │  │  │
│  │  │    "telegram_chat_id": {chat_id},            │  │  │
│  │  │    "linked_at": {now}                         │  │  │
│  │  │  }                                             │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ 5. Return Response:                            │  │  │
│  │  │                                                │  │  │
│  │  │  If Premium:                                  │  │  │
│  │  │  {                                             │  │  │
│  │  │    "success": true,                           │  │  │
│  │  │    "is_premium": true,                        │  │  │
│  │  │    "premium_until": "2026-12-31T23:59:59Z",  │  │  │
│  │  │    "message": "Synced!"                       │  │  │
│  │  │  }                                             │  │  │
│  │  │                                                │  │  │
│  │  │  If Not Premium:                              │  │  │
│  │  │  {                                             │  │  │
│  │  │    "success": true,                           │  │  │
│  │  │    "is_premium": false,                       │  │  │
│  │  │    "note": "No premium found"                 │  │  │
│  │  │  }                                             │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────┬───────────────────────┘
                                      │
                                      │ Read & Write
                                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    Data Layer / Storage                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────┐                        │
│  │    data/bot_users.json         │                        │
│  │  (from telegram_bot.py)         │                        │
│  ├────────────────────────────────┤                        │
│  │ {                              │                        │
│  │   "987654321": {               │                        │
│  │     "user_id": "john",         │                        │
│  │     "first_name": "John",      │                        │
│  │     "username": "johndoe",     │                        │
│  │     "expiry": "2026-12-31...", │  ← Premium Check      │
│  │     "stripe_subscription": ..., │                        │
│  │     ...                        │                        │
│  │   },                           │                        │
│  │   "555666777": { ... },        │                        │
│  │   ...                          │                        │
│  │ }                              │                        │
│  └────────────────────────────────┘                        │
│                                                             │
│  ┌────────────────────────────────┐                        │
│  │  Supabase Database             │                        │
│  │  (users table)                 │                        │
│  ├────────────────────────────────┤                        │
│  │ - user_id (PK)                 │                        │
│  │ - subscription_status          │  ← Updated here       │
│  │ - premium_from_telegram        │                        │
│  │ - telegram_chat_id             │                        │
│  │ - telegram_premium_until       │                        │
│  │ - linked_at                    │                        │
│  │ - ...                          │                        │
│  └────────────────────────────────┘                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## User Journey Timeline

```
T0: User opens app
├─ Goes to Profile Screen
└─ Scrolls to Integrations section

T1: User taps "Telegram Bot" row
├─ Telegram linking modal opens
└─ Sees two input fields

T2: User opens Telegram bot
├─ Clicks "🤖 Open HollowScan Bot" button
├─ Telegram opens to @HollowscanBot
└─ User sends /start command

T3: Telegram bot responds
├─ Bot sends message with chat ID
├─ User copies chat ID
└─ Returns to HollowScan app

T4: User enters data
├─ Pastes chat ID (e.g., "987654321")
├─ Enters username (e.g., "@johndoe")
└─ Clicks "Link Account"

T5: App processes request
├─ Shows loading spinner
├─ Sends: POST /v1/user/telegram/link?user_id=...&telegram_chat_id=987654321
└─ Waits for response

T6: Backend checks Telegram data
├─ Loads data/bot_users.json
├─ Finds user with chat_id = "987654321"
├─ Checks if expiry date is in future
├─ If yes → sets is_premium = true
└─ If no → sets is_premium = false

T7: Backend updates user DB
├─ If premium: Updates subscription_status to "active"
├─ Always: Stores telegram_chat_id
└─ Returns response

T8: Frontend receives response
├─ If is_premium = true:
│  ├─ Sets isPremium state to true
│  ├─ Sets premiumUntil state
│  ├─ Shows "🎉 Welcome to Premium!" alert
│  └─ Shows premium badge in UI
├─ If is_premium = false:
│  ├─ Shows "Account Linked" message
│  └─ Suggests upgrading on Telegram bot
└─ Closes modal

T9: User sees updated Integrations section
├─ If premium: Shows "👑 Premium until 12/31/2026"
├─ If linked: Shows "Receiving notifications"
└─ All premium features now available
```

---

## Data Flow Sequence

```
App User                Backend                 Telegram Bot Data
    │                     │                            │
    │  1. POST request    │                            │
    ├────────────────────>│                            │
    │   chat_id=987654321 │                            │
    │   user_id=user-123  │                            │
    │                     │  2. Load JSON              │
    │                     ├───────────────────────────>│
    │                     │                    bot_users.json
    │                     │  3. Find user entry        │
    │                     │<──────────────────────────┤
    │                     │  User data + expiry date   │
    │                     │                            │
    │                     │  4. Check expiry > now()   │
    │                     │     is_premium = true      │
    │                     │                            │
    │                     │  5. Update Supabase        │
    │                     ├──────> users table        │
    │                     │        subscription_status │
    │                     │        = "active"          │
    │                     │                            │
    │  6. Return response │                            │
    │<────────────────────┤                            │
    │  {                  │                            │
    │   is_premium: true  │                            │
    │   premium_until:... │                            │
    │  }                  │                            │
    │                     │                            │
    │  7. Update UI       │                            │
    ├──────┐              │                            │
    │      │ State update │                            │
    │      │ Show badge   │                            │
    │      │ Show alert   │                            │
    │<─────┘              │                            │
    │                     │                            │
    │  ✅ Complete!       │                            │
    │                     │                            │
```

---

## Premium Status Check Algorithm

```
Function: CheckTelegramPremium(chat_id)

Input: chat_id (integer, from user)

Step 1: Load bot users
  ├─ Open: data/bot_users.json
  └─ Read into: telegram_users dict

Step 2: Look up user
  ├─ Convert: chat_id to string
  ├─ Search: telegram_users[chat_id_str]
  └─ Result: user_data (dict or empty)

Step 3: Check premium field
  ├─ If 'expiry' not in user_data:
  │  └─ Return: is_premium = False (no premium)
  │
  └─ If 'expiry' in user_data:
     ├─ Parse: expiry_str to datetime object
     ├─ Handle: ISO8601 format (with 'Z' timezone)
     ├─ Replace: 'Z' with '+00:00' for Python parsing
     ├─ Get: current time (now, UTC)
     ├─ Compare: expiry_date > now?
     │
     ├─ If YES:
     │  └─ Return: is_premium = True ✅
     │
     └─ If NO:
        └─ Return: is_premium = False ❌

Output: boolean (True = premium, False = not premium)

Exceptions: 
  ├─ If JSON file not found → is_premium = False
  ├─ If user not in JSON → is_premium = False  
  └─ If date parsing fails → is_premium = False
```

---

## State Transitions

```
┌─────────────────────────┐
│   Not Linked State      │
│  (Initial)              │
│  ├─ telegramLinked=F    │
│  ├─ isPremium=F         │
│  └─ premiumUntil=null   │
└────────────┬────────────┘
             │ User clicks "Telegram Bot"
             ▼
┌─────────────────────────┐
│   Modal Opens           │
│  (Accepting Input)      │
│  ├─ telegramChatId=null │
│  └─ isLinking=F         │
└────────────┬────────────┘
             │ User enters data and clicks "Link"
             ▼
┌─────────────────────────┐
│   Linking In Progress   │
│  (API Call)             │
│  └─ isLinking=T         │
└────────────┬────────────┘
             │ Backend response received
             │ (splits into two paths)
             │
       ┌─────┴─────┐
       │           │
    Premium     Not Premium
       │           │
       ▼           ▼
┌──────────────┐ ┌────────────────┐
│ Premium Path │ │ Standard Path   │
├──────────────┤ ├────────────────┤
│ isPremium=T  │ │ isPremium=F    │
│ premiumUntil │ │ premiumUntil=null
│ =<date>      │ └────────────────┘
└──────┬───────┘         │
       │                 │
       │ Both paths:     │
       │ ├─ telegramLinked=T
       │ ├─ telegramChatId=<id>
       │ ├─ modal closes
       │ └─ Integrations section updates
       │                 │
       └────────┬────────┘
                ▼
     ┌──────────────────────┐
     │  Linked + Premium    │  or  │  Linked + Free    │
     │  (Success)           │      │  (Success)        │
     │  ├─ UI shows badge   │      │  ├─ UI shows      │
     │  │  "👑 Premium"     │      │  │  "✓ Linked"   │
     │  └─ Premium features │      │  └─ Free features │
     │     unlocked         │      │     available     │
     └──────────────────────┘      └───────────────────┘
```

---

## Integration Points

```
┌──────────────────────────┐
│  Telegram Bot Process    │
│ (telegram_bot.py)        │
│ ┌──────────────────────┐ │
│ │ User subscribes or   │ │
│ │ renews premium       │ │
│ └──────────────────────┘ │
│              │           │
│              │ Stripe    │
│              │ webhook   │
│              ▼           │
│ ┌──────────────────────┐ │
│ │ Update bot_users.json│ │
│ │ Set expiry date      │ │
│ └──────────────────────┘ │
└────────────────┬─────────┘
                 │
         (JSON file available)
                 │
                 ▼
         ┌──────────────────┐
         │   Mobile App     │
         │  (Expo/RN)       │
         │ ┌──────────────┐ │
         │ │ User links   │ │
         │ │ Telegram     │ │
         │ └──────────────┘ │
         │         │        │
         │         ▼        │
         │ ┌──────────────┐ │
         │ │ Call backend │ │
         │ │ endpoint     │ │
         │ └──────────────┘ │
         └────────────┬──────┘
                      │
                      ▼
         ┌────────────────────────┐
         │   FastAPI Backend      │
         │  (main_api.py)         │
         │ ┌────────────────────┐ │
         │ │ /v1/user/telegram/ │ │
         │ │ link endpoint      │ │
         │ ├────────────────────┤ │
         │ │ 1. Read bot_users  │ │
         │ │ 2. Check premium   │ │
         │ │ 3. Update DB       │ │
         │ │ 4. Return status   │ │
         │ └────────────────────┘ │
         │         │              │
         │         ▼              │
         │ ┌────────────────────┐ │
         │ │ Supabase Database  │ │
         │ │ User subscription  │ │
         │ │ status updated     │ │
         │ └────────────────────┘ │
         └──────────────────────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │  Mobile App Frontend   │
         │  ├─ State updated      │
         │  ├─ UI refreshed       │
         │  └─ Premium activated  │
         └────────────────────────┘
```

---

## Error Handling Flow

```
┌──────────────────────────┐
│  User Initiates Linking  │
└────────────┬─────────────┘
             ▼
┌──────────────────────────┐
│  Validation             │
├──────────────────────────┤
│ Check: chat_id exists?   │
│ Check: username exists?  │
└────────┬────────────────┘
         │
    ┌────┴─────┐
    │ (YES)    │ (NO/Empty)
    ▼          ▼
Success   ┌──────────────┐
          │ Show Alert:  │
          │ "Please      │
          │  enter data" │
          └──────────────┘
    │
    ▼
┌──────────────────────────┐
│  Backend Processing      │
├──────────────────────────┤
│ Load bot_users.json?     │
└────────┬────────────────┘
         │
    ┌────┴─────┐
    │ (YES)    │ (NO)
    ▼          ▼
Success   ┌──────────────┐
          │ is_premium=F │
          │ (fallback)   │
          └──────────────┘
    │
    ▼
┌──────────────────────────┐
│  Find User in JSON       │
├──────────────────────────┤
│ User[chat_id] exists?    │
└────────┬────────────────┘
         │
    ┌────┴─────┐
    │ (YES)    │ (NO)
    ▼          ▼
Success   ┌──────────────┐
          │ is_premium=F │
          │ Account      │
          │ linked only  │
          └──────────────┘
    │
    ▼
┌──────────────────────────┐
│  Check Expiry Date       │
├──────────────────────────┤
│ Parse date successfully? │
└────────┬────────────────┘
         │
    ┌────┴─────┐
    │ (YES)    │ (NO/Error)
    ▼          ▼
Success   ┌──────────────┐
          │ is_premium=F │
          │ (fallback)   │
          └──────────────┘
    │
    ▼
┌──────────────────────────┐
│  Compare: expiry > now?  │
├──────────────────────────┤
│ Is date in future?       │
└────────┬────────────────┘
         │
    ┌────┴──────┐
    │ (YES)     │ (NO/Expired)
    ▼           ▼
Premium=T  Premium=F
    │           │
    └─────┬─────┘
          ▼
    ┌──────────────┐
    │ Update User  │
    │ DB           │
    └─────┬────────┘
          ▼
    ┌──────────────┐
    │ Return       │
    │ Response     │
    └─────┬────────┘
          ▼
    ┌──────────────────────┐
    │ Frontend Updates UI  │
    └──────────────────────┘
```

---

This architecture ensures:
✅ Clear data flow from Telegram → App  
✅ Robust error handling with sensible defaults  
✅ No double billing or conflicts  
✅ Easy debugging with clear logging points  
✅ Scalable for future enhancements
